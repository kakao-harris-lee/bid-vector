# 서비스 로드맵 (bid-vector)

> 이 문서는 운영자가 정리한 0~3단계 로드맵을 **현재 코드 구현 상태와 대조**해 보강하고,
> 단계별 수용 기준·교차 워크스트림·리스크를 추가한 단일 출처 로드맵입니다.
> 기획 매핑은 `docs/first_plan_implementation_review.md`, 운영 원칙은 `AGENT.md`,
> 프론트 확장은 `docs/web-development-plan.md`를 함께 참고하세요.
>
> 최초 작성: 2026-06-04(PR #57). **갱신: 2026-06-16 — 현재 main(#58~#95) 기준 재대조.**

---

## 0.5 갱신 노트 (2026-06-16) — "빌드는 사실상 끝났다, 남은 건 검증이다"

> 최초 로드맵(2026-06-04, PR #57)은 **그 이후 머지된 #58~#95 작업의 거의 전부를 예언했지만, 문서는 갱신되지 않았다.**
> 즉 로드맵이 "다음에 빌드해야 한다"고 적은 항목들(C-1 계측·C-2 시공능력·C-3 개찰추적·공사특화리스크·등록 마법사)은
> **이미 구현돼 main에 들어가 있다.** 코드 재대조로 확인한 핵심 변동:

| 로드맵 원본 주장(2026-06-04) | 2026-06-16 실제 | 근거 |
|---|---|---|
| C-1 KPI 계측 = "측정 수단 없음, 가장 중요한 누락" | ✅ **구현됨** — operations-kpi 엔드포인트 + KPI 패널 + `project_view`/`recommendation_feedback` 이벤트 송신 + 피드백 라벨 export | #58·#59·#60·#61·#64·#69·#74 |
| C-2 시공능력 = "전용 필드 없음(프록시뿐)" | ✅ **구현됨** — `construction_capacity_amount`·`awarded_contract_limit` 컬럼 + 매칭/리스크 반영 | `models.py:143-150`, `classifier.py`, `opportunity_analysis.py` |
| C-3 개찰 자동 추적 = "자동 수집 태스크 부재" | ✅ **구현됨** — scsbid forward 수집 + `settle_forward_paper_bids` 자동 정산(이 호스트 ON) | #77·#78·#82 |
| 공사 특화 리스크 = "classifier에 로직 없음(❌)" | ✅ **구현됨** — 지역의무공동도급/공동도급/도급한도/유사실적 리스크 탐지 | `opportunity_analysis.py:32-44,115-137` |
| 등록 마법사 = "단일 폼(마법사 아님)" | ✅ **구현됨** — 4단계 guided wizard(첫 등록 시 자동 진입) | `CompanyInfoEditor.tsx:28-36,123` |
| 투찰가 추천의 조언/면책 포지셔닝(C-4) | ✅ **정직 명세 정렬** — probability=가격 적합도(추정), 투찰서=초안(자동제출 없음) | #88~#95, `CLAUDE.md §1.5` |

**재프레이밍된 결론:** 0~2단계는 **빌드가 사실상 완료**됐다. 유일하게 *검증 가능하게 미완성인 빌드 항목*은
**0단계의 운영 검증 smoke 리포트(+스케줄 활성화)** 하나다. 그 외 0~2단계에 남은 것은 거의 전부
*만드는 일*이 아니라 **실사용 데이터로 측정·증명하는 일**(1단계/2단계 exit gate, G-3)이다.
3단계 SaaS(멀티테넌트·RBAC·과금·월간 리포트)는 여전히 미구현이며 **G-3 게이트 전 착수 금지**(원칙 유지).

→ **다음 빌드 후보(우선순위순):** ①0단계 운영 검증 리포트 ②(검증 결과에 따라) 공사 매칭 스코어링 정밀화·코드마스터 드롭다운 등 소규모 정밀화 ③G-3 통과 후 3단계 SaaS.
하지만 **빌드보다 먼저 권장되는 것은 이미 만든 KPI/리포트로 실사용 증적을 모으는 것**이다(observation-mode).

---

## 0. 가이딩 원칙 (전 단계 공통)

1. **업체 정보 정확도 > AI 모델.** 추천 품질을 좌우하는 1순위는 예측 모델이 아니라
   업체 정보(면허·지역·공사가능금액·실적·선호/제외)의 정확도다. 모든 단계에서 이 5축이
   실제 매칭/추천에 반영되는지 우선 확인한다.
2. **데이터 해자(moat)는 "축적된 실제 개찰 결과".** 추천이 맞았는지 검증·학습하려면
   개찰 결과를 자동으로 수집·정합하는 파이프라인이 핵심 자산이다.
3. **범위 고정 — "공사 입찰 중소업체" 한정.** 기능을 넓히기보다 한 도메인에서 깊게.
   (운영자 원안의 핵심 직관, 유지.)
4. **증적 기반으로 진행.** 단계 전환은 "느낌"이 아니라 측정된 KPI와 산출물로 판단한다.
   → KPI를 측정할 **계측(telemetry)** 이 없으면 KPI는 목표가 아니라 희망이다 (교차 워크스트림 C-1).
5. **현재 구현이 기획보다 우선.** 충돌 시 호환 가능한 중간 단계부터.

---

## 1. 현재 구현 상태 요약 (2026-06-16, main 기준 재대조)

코드 재조사 결과 단계별 완성도. (✅ 완료 · ⚠️ 부분 · ❌ 미구현) — 괄호 안은 2026-06-04 원본 수치.

| 단계 | 완성도 | 한 줄 평 |
|---|---|---|
| **0단계 — 안정화** | ✅ 약 95% (←90%) | end-to-end 동작. 남은 *빌드*는 **smoke 스케줄 활성화 + 통합 운영 검증 리포트** 하나. 나머지는 운영 검증·증적. |
| **1단계 — 단일 업체 MVP** | ✅ 약 95% (←85%) | 시공능력 필드·KPI 계측·등록 마법사·프로필 인지 매칭·참가 사유 **모두 구현 완료**. 남은 건 빌드가 아니라 **실제 업체 2~4주 투입 + KPI 측정**(+ 선택: 코드마스터 드롭다운). |
| **2단계 — 실증 고도화** | ✅ 약 85% (←60%) | 개찰 자동 추적·공사 특화 리스크·피드백 라벨 루프·threshold 튜닝 제안 **구현됨**. 남은 건 **G-3 실측 가치 증명**(운영) + 소규모 정밀화(도급한도/지역가산 *스코어링* 반영). |
| **3단계 — SaaS 전환** | ⚠️ 약 30% | 단일 운영자 모델이 견고(= 멀티테넌트로 깨야 함). JWT 인증만 있고 **RBAC·과금·온보딩·월간 리포트 부재(미구현 확인)**. synthetic 다중 operator가 디딤돌. **G-3 전 착수 금지.** |

> 결론(2026-06-16): **0~2단계는 빌드가 사실상 완료됐다.** 진짜 병목은 *만드는 것*이 아니라
> *실제 업체에 붙여 측정·검증하는 것*이다(1·2단계 exit gate). 유일하게 남은 0~2단계 빌드 항목은
> **0단계 통합 운영 검증 리포트** 하나. 3단계 SaaS는 **2단계 가치 검증(G-3) 전에 시작하지 말 것**.

---

## 2. 단계별 로드맵 (보강판)

각 단계: **목표 / 현재 상태 / 남은 작업 / 수용 기준(Exit Gate)**.

### 0단계 — 현재 코드 안정화 → *운영 검증·증적*

**목표:** "개발된 기능이 실제 환경에서 한 사이클 돈다 + 증적이 남는다."

**현재 상태 (코드 근거):**
- ✅ KONEPS 실수집: `app/services/koneps/collector.py` — OpenAPI(koneps/scsbid) 1차 + 크롤 fallback, `Project`/`HistoricalData`/`TenderResult` 적재, `collect_koneps_notices` Celery 태스크 + beat.
- ✅ Telegram: 송신(`telegram.py`) + 폴링(`poll_telegram_updates`, 현재 .env에서 ENABLED) + webhook + 발신자 인가 게이트(PR #53). 인바운드 버튼/명령으로 의사결정.
- ✅ Smoke test: `app/services/smoke_test.py` + 일일 스케줄(PR #49, `SMOKE_TEST_SCHEDULE_ENABLED`, 기본 OFF). 수집→예측→Telegram 4단계 phase.
- ✅ 대시보드: `app/api/dashboard.py`(`/summary`에 crawl_success_rate·notification 집계) + 프론트 HomeScreen/Opportunities/Results.
- ✅ 실패 증적: `CrawlJob`(status/error_message/result_count), `Notification`, `app/services/analytics_reporting.py`.

**남은 작업 (★ = 0~2단계 통틀어 유일하게 남은 *빌드* 항목):**
- [ ] **smoke 스케줄 활성화**(`SMOKE_TEST_SCHEDULE_ENABLED=true`, 현재 OFF) 후 며칠 자동 사이클 관찰. (Telegram 폴링은 이미 ON.) — 빌드 인프라(`build_smoke_test_beat_schedule`)는 이미 존재, 토글만 남음.
- [ ] ★ **smoke/운영 통합 리포트 영속·조회**: phase별 PASS/FAIL, 수집 성공률, 추천 공고 수, 알림 성공률을 한 곳에서 보는 운영 검증 리포트(현재는 로그/개별 모델·`analytics_reporting.py`의 crawl/strategy/task 대시보드에 흩어짐 — smoke 사이클 단위 통합본 부재).
- [ ] 예측/추천 API 응답 샘플 캡처(증적 보관).

**수용 기준(Exit):** 운영 smoke 리포트가 N일 연속 green이고, 수집 성공률·추천 수·알림 성공률이 한 화면/리포트로 확인 가능.

---

### 1단계 — 단일 업체 실증 MVP

**목표:** "한 공사 중소업체가 실제 입찰 업무에 매일 쓴다."

**대상:** 공사 분야 중소업체 1곳, 운영자 1명, 실제 업체 프로필.

**현재 상태 (코드 근거, 2026-06-16 재대조):**
- ✅ **업체 프로필 등록**: `CompanyProfile`(면허/지역/업종/연매출/역량/실적) + 프론트 `features/profile/CompanyInfoEditor`(PR #51) — **4단계 guided 마법사**(첫 등록 시 자동 진입: 면허→지역→능력→선호) + 이후 단일 폼 편집(`CompanyInfoEditor.tsx:28-36,123`). **시공능력평가액(`construction_capacity_amount`)·도급한도(`awarded_contract_limit`) 전용 필드 구현됨**(`models.py:143-150`).
- ✅ **맞춤 추천 + 참가 가능/불가 사유**: classifier 6축(업종/면허/지역/예산/역량/시맨틱) + 건설 면허 매칭(PR #52) + 시공능력 capacity 매칭(`classifier.py`) + `opportunity_analysis`의 strengths/risk_flags + `DecisionReasonsCard`(왜 가능/왜 위험/판단, PR #54~56). 백테스트도 프로필 인지(PR #50).
- ✅ **투찰 후보가 추천**: `app/ai/` predictor 3종 + 자동 선택 + guardrail(낙찰하한), 시나리오별 추천가, paper bidding. + 투찰 의사결정 요약/투찰서 초안 export(#94·#95).
- ✅ **알림 + 투찰 여부 기록**: `BidDecisionRecord`(action/decision_status/reasoning), Telegram 콜백 동기화.
- ✅ **KPI 계측(C-1)**: `project_view`/`recommendation_feedback` 이벤트 송신 + operations-kpi 집계 + `OperationsKpiPanel`(#58~#61·#64·#69·#74).

**남은 작업:**
- [x] ~~시공능력 도메인 필드 추가~~ — **구현됨**(`construction_capacity_amount`·`awarded_contract_limit`, `models.py:143-150`). *남은 정밀화*: `awarded_contract_limit`을 리스크 플래그(`opportunity_analysis._detect_awarded_contract_limit_risks`)뿐 아니라 **역량 스코어링**에도 반영(현재 risk-flag 수준).
- [x] ~~등록 마법사 UX~~ — **구현됨**(`CompanyInfoEditor` 4단계 wizard).
- [x] ~~KPI 계측(C-1)~~ — **구현됨**(이벤트 송신 + operations-kpi). *남은 일은 빌드가 아니라 N일 실사용으로 KPI에 데이터를 채우는 것.*
- [ ] (선택) 코드 마스터(KONEPS 업종/지역/면허) 드롭다운으로 자유텍스트 오입 제거 — 현재 정규식 추출(`classifier.py:87 LICENSE_CODE_PATTERN`)로 동작하나 드롭다운 미도입.
- [ ] **(운영, 빌드 아님) 실제 업체 1곳 2~4주 매일 사용 + 위 KPI를 측정값으로 수집** — 1단계 Exit의 본체.

**KPI:** 공고 검토 시간 70%↓ · 놓친 유효 공고 감소 · 추천 중 검토 가치 60%↑ · 매일 확인 가능한 대시보드.

**수용 기준(Exit):** 실제 업체 1곳이 2~4주간 매일 사용 + 위 KPI를 *측정값*으로 제시.

---

### 2단계 — 실증 고도화

**목표:** "추천이 맞는지, 투찰가가 도움이 되는지 데이터로 검증한다."

**현재 상태 (코드 근거, 2026-06-16 재대조):**
- ✅ 사정률(예가 대비) 예측 성능 추적: `PricePrediction.backtest_average_absolute_error_rate`, `predictor_backtest.py`, historical_backtest 자동 스케줄(누적).
- ✅ 피드백 보정 + threshold 조정: `prediction_feedback.py`(실제/예측 비교 보정), `OperatorStrategy` bid_now/review_threshold, `decision_analytics` 퍼널. + probability_score 실측 보정(#92).
- ✅ **개찰 결과 자동 추적**: scsbid forward 개찰결과 자동 수집(`KONEPS_SCSBID_COLLECTION_SCHEDULE_ENABLED`, 이 호스트 ON) + `settle_forward_paper_bids` 자동 정산(`jobs.py`) → `TenderResult`/`PaperBidSettlement` 정합(#77·#78·#82). (win rate는 여전히 "가격 기준 추정 낙찰" 프록시 — caveat 유지, `CLAUDE.md §1.5`.)
- ✅ **공사 특화 리스크**: 지역의무공동도급(`region_joint_venture`)·공동도급(`joint_venture`)·도급한도(`_detect_awarded_contract_limit_risks`)·유사실적 리스크를 사유에 반영(`opportunity_analysis.py:32-44,115-137`).

**남은 작업:**
- [x] ~~개찰 결과 자동 추적 파이프라인~~ — **구현됨**(scsbid forward 수집 + 자동 정산). *남은 정밀화*: 추천 vs 실제 / paper vs 실제를 한눈에 보는 **자동 정확도 리포트**(수집·정산은 자동, 통합 정확도 KPI export는 부분).
- [x] ~~공사 특화 리스크 모델링~~ — **구현됨**(공동도급/지역의무/도급한도/유사실적). *남은 정밀화*: 지역가산점·도급한도를 risk-flag를 넘어 **매칭 스코어**에 반영.
- [x] ~~추천 fp/fn 라벨 수집 루프~~ — **구현됨**(`recommendation_feedback` 이벤트 → `build_recommendation_feedback_labels` export, #69). *남은 일은 라벨에 실데이터를 채우는 운영.*
- [x] ~~업체별 전략 threshold 자동 튜닝 제안~~ — **구현됨**(`decision_experiments.apply_threshold_adjustments` + `build_recommendations` 제안).

**KPI:** 추천 정확도 · 실제 투찰 전환율 · 투찰 후 개찰결과 추적률 · 추천가-실제낙찰가 차이 · 수동 수정 횟수↓. **(계측 인프라는 구비 — 남은 건 실사용 데이터 축적.)**

**수용 기준(Exit·게이트 G-3):** 개찰 결과가 자동 추적되어 *실측* 추천 정확도/투찰가 오차가 산출되고, 1~3개 실제 업체에서 "쓸 만하다"가 데이터로 증명됨. → 이게 충족돼야 3단계(SaaS) 착수.

---

### 3단계 — 공사 입찰 특화 SaaS 전환

**목표:** "중소 공사업체용 SaaS로 전환." **범위 고정: "공사 입찰 중소업체"** (원안 유지).

**현재 상태 (코드 근거):**
- ✅/⚠️ **멀티테넌트 준비도**: 현재 *의도적으로* 단일 운영자(`CompanyProfile.user_id`/`OperatorStrategy.user_id` unique, `single_user.ensure_operator_account` canonical). SaaS는 이 가정을 깨야 함 → 테넌시·데이터 격리 필요. legacy `allocations` 다중 배분 흔적 잔존.
- ✅ **디딤돌**: synthetic 다중 operator 인프라(`synthetic_custom_operator.py`, operator별 profile/strategy upsert·조회 패턴)가 멀티테넌트 코드 시드로 활용 가능.
- ⚠️ **인증/과금/리포트**: JWT 발급(`auth.py`)은 있으나 RBAC(역할)·과금/구독 모델·온보딩 흐름·월간 리포트 생성 **전무**(`is_admin` 플래그만).

**남은 작업:**
- [ ] **멀티테넌트 아키텍처(최대 비용)**: user_id unique 해제 + 테넌트별 데이터 격리, canonical operator 가정 제거, KONEPS 수집은 공용/테넌트별 rate-limit 관리. → 단일→다중은 *대공사*. G-3 통과 후 착수.
- [ ] **RBAC + 온보딩 마법사**(1단계 등록 마법사를 테넌트 셀프서비스로 확장).
- [ ] **과금/구독 도메인** + 요금제 게이팅.
- [ ] **월간 성과 리포트** 생성(decision_analytics 퍼널을 리포트로 패키징).
- [ ] 공고 상세 AI 요약 · 투찰 이력 관리 · 개찰 결과 자동 추적(2단계 산출물 재사용).

**요금제 ↔ 현재 기능 매핑:**
| 티어 | 기능 | 현재 |
|---|---|---|
| **Lite** | 공고 추천/알림 | ✅ 대부분 구현 |
| **Pro** | 공고 분석/투찰가 추천 | ✅ 대부분 구현 |
| **Expert** | 실증 리포트/전략 튜닝/컨설팅 | ⚠️ 리포트·자동 튜닝 빌드 필요 |

→ Lite/Pro는 기존 기능으로 거의 패키징 가능. Expert는 2단계 산출물(개찰 추적·리포트)에 의존.

---

## 3. 교차 워크스트림 (단계에 걸친 필수 작업)

- **C-1. 계측/관측(telemetry).** ✅ **구현됨**(2026-06-16). `project_view`/`recommendation_feedback` 이벤트 송신 +
  operations-kpi 집계 + `OperationsKpiPanel`(#58~#61·#64·#69·#74). *남은 일은 인프라 빌드가 아니라 실사용으로 데이터를 채우는 것.*
- **C-2. 공사 도메인 모델.** ✅ **대부분 구현됨**. 시공능력평가액·도급한도 필드(1단계), 공동도급·지역의무·도급한도·유사실적 리스크(2단계).
  *남은 정밀화*: 지역가산점·도급한도를 risk-flag를 넘어 **매칭 스코어**에 반영. 이 도메인 정확도가 "공사 특화" 차별점의 본질.
- **C-3. 개찰 결과 데이터 파이프라인.** ✅ **구현됨**(#77·#78·#82). scsbid forward 자동 수집 + 자동 정산. 학습/리포트/Expert 티어의 토대.
  *남은 정밀화*: 추천 vs 실제 통합 정확도 리포트.
- **C-4. 컴플라이언스.** ⚠️ **부분**. 투찰가 추천의 *조언/면책* 포지셔닝은 정직 명세로 정렬됨(#88~#95, probability=추정·투찰서=초안·자동제출 없음, `CLAUDE.md §1.5`).
  KONEPS 크롤 ToS·rate limit(OpenAPI 우선, sleep) 유지. **멀티테넌트 사업자정보/개인정보는 3단계 전 정리 — 미착수.**

---

## 4. 리스크 레지스터

| 리스크 | 영향 | 완화 |
|---|---|---|
| KONEPS API/사이트 변경·ToS | 수집 중단 | OpenAPI 우선 경로 유지, 크롤은 fallback·저빈도, 변경 감지 알림 |
| 예측/추천 신뢰도가 "투찰가를 믿을" 수준에 못 미침 | 핵심 가치 훼손 | 2단계 실측 정확도 게이트, guardrail 유지, "조언" 포지셔닝 |
| 단일 실증 업체 의존 | 일반화 실패 | 2단계에서 1→3개 업체로 확장 후 SaaS |
| **2단계 검증 전 SaaS 착수** | 멀티테넌트 대공사 매몰비용 | **게이트 G-3 강제**: 실측 가치 증명 전 3단계 금지 |
| ~~공사 도메인 미반영~~ → 반영됐으나 정확도 미검증 | 공사업체에 "안 맞는" 추천 | ✅ C-2 구현(시공능력 필드·공동도급/지역의무/도급한도/유사실적 리스크). 잔여: 실사용으로 매칭 정확도 검증 + 도급한도/지역가산 스코어링 정밀화 |
| ~~KPI 미계측~~ → 계측됐으나 데이터 미축적 | 진척/가치 입증 불가 | ✅ C-1 구현(이벤트 송신+operations-kpi). 잔여: N일 실사용으로 KPI에 데이터 채우기 |

---

## 5. 제안 요약 (원안 대비 추가·변경점)

1. **0~1단계 재프레이밍**: 대부분 *이미 구현됨*. 0단계는 "빌드"가 아니라 **운영 검증·증적**,
   1단계는 "기능 개발"이 아니라 **실제 업체 투입 + KPI 측정**으로 본다.
2. **계측(C-1)을 1단계와 병행 추가**: KPI를 측정할 수단이 없으면 1~2단계가 검증 불가. *가장 중요한 누락.*
3. **시공능력 도메인 필드를 1단계로 승격**: 원안 1단계 "시공능력"은 현재 전용 필드가 없음(프록시뿐).
   공사 매칭 정확도 직결 → 명시적 빌드 항목.
4. **개찰 결과 자동 추적을 2단계 최우선 + 데이터 해자로 명시**: 현재 수동/백테스트뿐. 자산화.
5. **공사 특화 리스크(공동도급/지역의무/지역가산/유사실적)를 명시적 2단계 항목으로**: 현재 미구현이며
   "공사 특화 SaaS" 차별점의 핵심.
6. **G-3 게이트 신설**: 2단계 실측 가치 증명 전 3단계 SaaS 착수 금지. 멀티테넌트는 단일→다중 *대공사*임을 명시.
7. **요금제↔기능 매핑**: Lite/Pro는 기존 기능으로 거의 패키징 가능, Expert만 2단계 산출물 의존 — 단계 의존성 명확화.
8. **컴플라이언스(C-4)**: 투찰가 추천의 조언/면책 포지셔닝·KONEPS ToS·멀티테넌트 정보보호를 3단계 전 정리.

> **2026-06-16 후속:** 위 §5의 제안 2·3·4·5(계측·시공능력 필드·개찰 자동 추적·공사 특화 리스크)는 **모두 구현 완료**되어 main에 반영됨(#58~#95). 제안 8(조언/면책 포지셔닝)도 정직 명세로 정렬됨(#88~#95). 남은 미구현 제안은 6(3단계 SaaS, G-3 게이트로 보류)·7(요금제 패키징, 3단계).

---

## 6. 단계 의존성 한눈에 (2026-06-16 갱신)

```
0단계(운영 검증·증적)  ── 빌드 ~95%, 남은 빌드: ★smoke 스케줄 ON + 통합 운영 리포트
   │
1단계(단일 업체 MVP)   ── 빌드 ~95% 완료
   ├─ ✅ 시공능력 필드(C-2)
   ├─ ✅ 등록 마법사
   └─ ✅ KPI 계측(C-1)
   │   └─ ☐ (운영) 실제 업체 2~4주 사용 + KPI 측정값  ←★ 1단계 Exit의 본체
   │
2단계(실증 고도화)
   ├─ ✅ 개찰 자동 추적(C-3)
   ├─ ✅ 공사 리스크 모델(C-2)  · ✅ 피드백 라벨 루프 · ✅ threshold 튜닝 제안
   ├─ ☐ (정밀화) 도급한도/지역가산 → 매칭 스코어 · 추천 vs 실제 통합 정확도 리포트
   │  ════ 게이트 G-3: 실측 가치 증명(운영) ════  ←★ 진짜 병목
3단계(SaaS) ── ❌ 미착수(G-3 전 금지)
   ├─ 멀티테넌트(대공사) + RBAC
   ├─ 과금/온보딩
   └─ 월간 리포트/Expert (C-3 재사용)
```

**한 줄 요약(2026-06-16):** 다음 *빌드*는 **0단계 통합 운영 검증 리포트** 하나뿐. 그 외 0~2단계의 남은 일은 전부 **실사용 증적 수집(운영)**. 3단계는 G-3 게이트 통과 후.
