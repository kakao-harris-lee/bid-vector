/**
 * 자동 생성 placeholder.
 *
 * 백엔드 OpenAPI 스키마는 `/sync-types` 슬래시 커맨드로
 * `openapi-typescript`을 통해 이 파일에 생성한다. Phase 1 시작 시 첫 동기화를
 * 수행한다. 그 전까지는 도메인별 수기 타입(`shared/types/<domain>.ts`)을 사용.
 *
 * **이 파일을 손으로 수정하지 말 것.** 보조 타입은 별도 파일에 작성한다.
 */

export {};
